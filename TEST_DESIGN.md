# 重复文件分析工具测试设计方案

## 1. 测试框架选择

**使用 pytest 作为测试框架**，原因如下：
- **强大的 fixture 系统**：适合管理测试环境，如临时文件、数据库连接等
- **丰富的插件生态**：支持测试覆盖率报告、参数化测试等
- **灵活的断言语法**：易于编写清晰的测试用例
- **良好的错误报告**：便于调试测试失败
- **支持测试发现**：自动找到测试文件和测试函数

**测试依赖**：
- pytest
- pytest-cov
- pytest-mock
- tempfile
- os
- sqlite3

**安装命令**：
```bash
pip install pytest pytest-cov pytest-mock
```

## 2. 测试结构设计

```
tests/
├── conftest.py          # 测试配置和fixtures
├── test_core/           # 核心模块测试
│   ├── test_file_scanner.py
│   ├── test_hash_calculator.py
│   ├── test_index_manager.py
│   └── test_config_manager.py
├── test_commands/       # 命令行命令测试
│   ├── test_index.py
│   ├── test_show.py
│   ├── test_hash.py
│   ├── test_export.py
│   ├── test_config.py
│   ├── test_db.py
│   └── test_clean.py
├── test_database/       # 数据库操作测试
│   ├── test_connection.py
│   ├── test_tables.py
│   └── test_queries.py
├── test_filesystem/     # 文件系统操作测试
│   ├── test_file_operations.py
│   ├── test_path_handling.py
│   └── test_hash_operations.py
└── utils/               # 测试辅助工具
    ├── mock_filesystem.py
    ├── mock_database.py
    └── test_helpers.py
```

## 3. 交互式命令行的自动补全测试

### 3.1 命令补全测试

#### 测试用例 1：根命令补全
- **测试目标**：验证根命令补全功能
- **测试步骤**：
  1. 模拟用户输入部分根命令（如 "i"）
  2. 验证补全建议包含 "index"
  3. 模拟用户输入部分根命令（如 "s"）
  4. 验证补全建议包含 "show"
- **预期结果**：补全建议包含所有匹配的根命令

#### 测试用例 2：子命令补全
- **测试目标**：验证子命令补全功能
- **测试步骤**：
  1. 模拟用户输入 "index "（带空格）
  2. 验证补全建议包含 "scan", "import", "rebuild", "clear"
  3. 模拟用户输入 "show "（带空格）
  4. 验证补全建议包含 "groups", "files", "hash", "stats"
- **预期结果**：补全建议包含所有匹配的子命令

#### 测试用例 3：参数补全
- **测试目标**：验证参数补全功能
- **测试步骤**：
  1. 模拟用户输入 "index scan "（带空格）
  2. 验证补全建议包含有效的路径
  3. 模拟用户输入 "show files "（带空格）
  4. 验证补全建议包含有效的文件模式
- **预期结果**：补全建议包含有效的参数

### 3.2 补全功能边界测试

#### 测试用例 4：空输入补全
- **测试目标**：验证空输入时的补全行为
- **测试步骤**：
  1. 模拟用户输入空字符串并触发补全
  2. 验证补全建议包含所有根命令
- **预期结果**：补全建议包含所有根命令

#### 测试用例 5：部分输入补全
- **测试目标**：验证部分输入时的补全行为
- **测试步骤**：
  1. 模拟用户输入 "ind" 并触发补全
  2. 验证补全建议包含 "index"
  3. 模拟用户输入 "sha" 并触发补全
  4. 验证补全建议包含 "show"
- **预期结果**：补全建议包含所有匹配的命令

#### 测试用例 6：无效输入补全
- **测试目标**：验证无效输入时的补全行为
- **测试步骤**：
  1. 模拟用户输入 "xyz" 并触发补全
  2. 验证补全建议为空
- **预期结果**：补全建议为空

## 4. 指令本身的交互信息测试

### 4.1 命令帮助信息测试

#### 测试用例 7：根命令帮助信息
- **测试目标**：验证根命令帮助信息
- **测试步骤**：
  1. 执行 "help" 命令
  2. 验证输出包含所有根命令及其说明
- **预期结果**：输出包含所有根命令及其说明

#### 测试用例 8：子命令帮助信息
- **测试目标**：验证子命令帮助信息
- **测试步骤**：
  1. 执行 "help index" 命令
  2. 验证输出包含 index 命令的所有子命令及其说明
  3. 执行 "help show" 命令
  4. 验证输出包含 show 命令的所有子命令及其说明
- **预期结果**：输出包含指定命令的所有子命令及其说明

### 4.2 命令执行反馈测试

#### 测试用例 9：命令执行成功反馈
- **测试目标**：验证命令执行成功的反馈信息
- **测试步骤**：
  1. 执行 "version" 命令
  2. 验证输出包含版本信息
- **预期结果**：输出包含版本信息

#### 测试用例 10：命令执行失败反馈
- **测试目标**：验证命令执行失败的错误信息
- **测试步骤**：
  1. 执行不存在的命令
  2. 验证输出包含错误信息
- **预期结果**：输出包含错误信息

#### 测试用例 11：命令执行进度信息
- **测试目标**：验证命令执行进度信息
- **测试步骤**：
  1. 执行 "index scan" 命令（模拟）
  2. 验证输出包含扫描进度信息
- **预期结果**：输出包含扫描进度信息

### 4.3 交互式模式测试

#### 测试用例 12：命令输入和执行流程
- **测试目标**：验证命令输入和执行流程
- **测试步骤**：
  1. 模拟用户输入 "version" 命令
  2. 验证命令执行并输出正确结果
  3. 模拟用户输入 "exit" 命令
  4. 验证程序退出
- **预期结果**：命令执行正确，程序正常退出

#### 测试用例 13：历史命令功能
- **测试目标**：验证历史命令功能
- **测试步骤**：
  1. 模拟用户输入多个命令
  2. 模拟用户使用上下箭头浏览历史命令
  3. 验证历史命令正确显示
- **预期结果**：历史命令正确显示

## 5. 功能模块测试

### 5.1 扫描与索引模块测试

#### 测试用例 14：文件扫描功能
- **测试目标**：验证文件扫描功能
- **测试步骤**：
  1. 创建测试目录和文件
  2. 执行文件扫描
  3. 验证扫描结果正确
- **预期结果**：扫描结果包含所有测试文件

#### 测试用例 15：路径排除功能
- **测试目标**：验证路径排除功能
- **测试步骤**：
  1. 创建测试目录和文件，包含需要排除的文件
  2. 配置排除模式
  3. 执行文件扫描
  4. 验证排除的文件未被扫描
- **预期结果**：排除的文件未被扫描

#### 测试用例 16：CSV导入功能
- **测试目标**：验证CSV导入功能
- **测试步骤**：
  1. 创建测试CSV文件
  2. 执行CSV导入
  3. 验证导入结果正确
- **预期结果**：导入结果包含CSV文件中的所有文件

#### 测试用例 17：索引重建功能
- **测试目标**：验证索引重建功能
- **测试步骤**：
  1. 创建测试文件
  2. 执行索引重建
  3. 验证索引重建成功
- **预期结果**：索引重建成功，包含所有测试文件

#### 测试用例 18：重复文件组计算
- **测试目标**：验证重复文件组计算
- **测试步骤**：
  1. 创建重复文件
  2. 执行重复文件组计算
  3. 验证重复文件组正确
- **预期结果**：重复文件组包含所有重复文件

#### 测试用例 19：索引清理功能
- **测试目标**：验证索引清理功能
- **测试步骤**：
  1. 创建测试文件
  2. 删除部分测试文件
  3. 执行索引清理
  4. 验证清理后的索引正确
- **预期结果**：清理后的索引不包含已删除的文件

### 5.2 哈希计算模块测试

#### 测试用例 20：文件哈希计算
- **测试目标**：验证文件哈希计算
- **测试步骤**：
  1. 创建测试文件
  2. 计算文件哈希
  3. 验证哈希值正确
- **预期结果**：哈希值正确

#### 测试用例 21：哈希验证功能
- **测试目标**：验证哈希验证功能
- **测试步骤**：
  1. 创建测试文件
  2. 计算文件哈希
  3. 验证哈希值
  4. 修改文件
  5. 再次验证哈希值
- **预期结果**：修改后的文件哈希值不同

#### 测试用例 22：哈希备份和恢复
- **测试目标**：验证哈希备份和恢复功能
- **测试步骤**：
  1. 创建测试文件并计算哈希
  2. 备份哈希值
  3. 清除哈希值
  4. 恢复哈希值
  5. 验证恢复后的哈希值正确
- **预期结果**：恢复后的哈希值正确

#### 测试用例 23：大文件处理
- **测试目标**：验证大文件处理功能
- **测试步骤**：
  1. 创建大测试文件
  2. 计算文件哈希
  3. 验证哈希计算成功
- **预期结果**：大文件哈希计算成功

### 5.3 显示数据模块测试

#### 测试用例 24：重复文件组列表显示
- **测试目标**：验证重复文件组列表显示
- **测试步骤**：
  1. 创建重复文件
  2. 执行重复文件组列表显示
  3. 验证显示结果正确
- **预期结果**：显示结果包含所有重复文件组

#### 测试用例 25：文件查询功能
- **测试目标**：验证文件查询功能
- **测试步骤**：
  1. 创建测试文件
  2. 执行文件查询
  3. 验证查询结果正确
- **预期结果**：查询结果包含匹配的文件

#### 测试用例 26：哈希查询功能
- **测试目标**：验证哈希查询功能
- **测试步骤**：
  1. 创建测试文件并计算哈希
  2. 执行哈希查询
  3. 验证查询结果正确
- **预期结果**：查询结果包含匹配哈希值的文件

#### 测试用例 27：统计分析功能
- **测试目标**：验证统计分析功能
- **测试步骤**：
  1. 创建测试文件
  2. 执行统计分析
  3. 验证统计结果正确
- **预期结果**：统计结果正确

### 5.4 导出模块测试

#### 测试用例 28：CSV格式导出
- **测试目标**：验证CSV格式导出
- **测试步骤**：
  1. 创建测试文件
  2. 执行CSV导出
  3. 验证导出文件格式和内容正确
- **预期结果**：导出文件格式和内容正确

#### 测试用例 29：JSON格式导出
- **测试目标**：验证JSON格式导出
- **测试步骤**：
  1. 创建测试文件
  2. 执行JSON导出
  3. 验证导出文件格式和内容正确
- **预期结果**：导出文件格式和内容正确

#### 测试用例 30：详细报告生成
- **测试目标**：验证详细报告生成
- **测试步骤**：
  1. 创建测试文件
  2. 执行详细报告生成
  3. 验证报告内容正确
- **预期结果**：报告内容正确

### 5.5 配置模块测试

#### 测试用例 31：扫描范围限制设置
- **测试目标**：验证扫描范围限制设置
- **测试步骤**：
  1. 设置扫描范围限制
  2. 执行文件扫描
  3. 验证扫描范围正确
- **预期结果**：扫描范围符合限制

#### 测试用例 32：排除模式管理
- **测试目标**：验证排除模式管理
- **测试步骤**：
  1. 添加排除模式
  2. 验证排除模式已添加
  3. 删除排除模式
  4. 验证排除模式已删除
- **预期结果**：排除模式管理正确

#### 测试用例 33：配置文件读写
- **测试目标**：验证配置文件读写
- **测试步骤**：
  1. 修改配置
  2. 验证配置文件已更新
  3. 读取配置
  4. 验证读取的配置正确
- **预期结果**：配置文件读写正确

### 5.6 数据库模块测试

#### 测试用例 34：数据库连接和初始化
- **测试目标**：验证数据库连接和初始化
- **测试步骤**：
  1. 初始化数据库
  2. 验证数据库连接成功
  3. 验证表结构正确
- **预期结果**：数据库连接和初始化成功

#### 测试用例 35：CRUD操作
- **测试目标**：验证CRUD操作
- **测试步骤**：
  1. 创建测试数据
  2. 读取测试数据
  3. 更新测试数据
  4. 删除测试数据
  5. 验证操作结果正确
- **预期结果**：CRUD操作正确

#### 测试用例 36：数据库备份和恢复
- **测试目标**：验证数据库备份和恢复
- **测试步骤**：
  1. 创建测试数据
  2. 备份数据库
  3. 修改测试数据
  4. 恢复数据库
  5. 验证恢复后的数据库正确
- **预期结果**：数据库备份和恢复正确

### 5.7 清理模块测试

#### 测试用例 37：重复文件清理功能
- **测试目标**：验证重复文件清理功能
- **测试步骤**：
  1. 创建重复文件
  2. 执行重复文件清理（模拟）
  3. 验证清理结果正确
- **预期结果**：清理结果正确

#### 测试用例 38：清理脚本生成
- **测试目标**：验证清理脚本生成
- **测试步骤**：
  1. 创建重复文件
  2. 生成清理脚本
  3. 验证脚本内容正确
- **预期结果**：脚本内容正确

#### 测试用例 39：模拟运行模式
- **测试目标**：验证模拟运行模式
- **测试步骤**：
  1. 创建重复文件
  2. 执行模拟运行
  3. 验证模拟结果正确
  4. 验证文件未被删除
- **预期结果**：模拟结果正确，文件未被删除

## 6. 测试执行策略

### 6.1 测试执行顺序
1. 单元测试（功能模块）
2. 命令行命令测试
3. 数据库操作测试
4. 文件系统操作测试
5. 交互式命令行测试

### 6.2 测试环境配置
- 使用临时目录和文件
- 使用内存数据库
- 模拟文件系统操作
- 模拟用户输入

### 6.3 测试覆盖率目标
- 核心功能模块：90% 以上
- 命令行命令：85% 以上
- 数据库操作：80% 以上
- 整体覆盖率：85% 以上

### 6.4 测试运行命令

**运行所有测试**：
```bash
pytest
```

**运行特定模块测试**：
```bash
pytest tests/test_core/test_file_scanner.py
```

**生成覆盖率报告**：
```bash
pytest --cov=commands --cov-report=html
```

**运行测试并显示详细输出**：
```bash
pytest -v
```

## 7. 测试辅助工具

### 7.1 模拟文件系统
- 创建临时文件和目录
- 模拟文件系统操作
- 模拟文件权限和属性

### 7.2 模拟数据库
- 创建内存数据库
- 模拟数据库操作
- 预设测试数据

### 7.3 测试辅助函数
- 生成测试文件
- 生成测试数据
- 验证测试结果

## 8. 最佳实践

1. **隔离测试环境**：使用临时目录和内存数据库
2. **模拟外部依赖**：模拟文件系统和数据库操作
3. **测试边界条件**：测试空输入、无效输入、边界情况
4. **测试错误处理**：测试异常情况和错误恢复
5. **持续集成**：在 CI/CD 流程中运行测试
6. **定期更新测试**：随着代码变更更新测试用例
7. **测试文档**：编写清晰的测试文档和注释

通过以上测试设计，可以全面覆盖重复文件分析工具的所有功能和场景，确保系统的稳定性和可靠性。